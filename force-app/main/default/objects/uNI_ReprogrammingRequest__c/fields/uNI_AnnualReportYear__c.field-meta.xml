<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>uNI_AnnualReportYear__c</fullName>
    <description>this field contains the cutoff year, if reprogramming request is approved before the cutoff date, of that year (31-June) the annual report considers this budget, else , this field is set to next year.</description>
    <externalId>false</externalId>
    <formula>IF(
    uNI_ApprovedDate__c &gt; DATE(YEAR(uNI_ApprovedDate__c), 6, 30),
    TEXT(YEAR(uNI_ApprovedDate__c) + 2),
    TEXT(YEAR(uNI_ApprovedDate__c) + 1)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Annual Report Year</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
